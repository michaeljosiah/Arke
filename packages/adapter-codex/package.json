{
  "name": "@arke/adapter-codex",
  "version": "0.1.0",
  "private": true,
  "description": "Second leaf HarnessAdapter (SPEC-034): drives OpenAI Codex over the `codex app-server` JSON-RPC 2.0/stdio protocol. Proves the neutral adapter seam against a transport that shares nothing with OpenCode's HTTP/SSE.",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test \"test/**/*.test.ts\""
  },
  "dependencies": {
    "@arke/contracts": "*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}

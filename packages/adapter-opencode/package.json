{
  "name": "@arke/adapter-opencode",
  "version": "0.1.0",
  "private": true,
  "description": "The first harness adapter: maps Arke capabilities onto OpenCode's headless server API and SSE event stream (PRD §15). Normalizes provider-native events into the canonical domain model.",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@arke/contracts": "*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}

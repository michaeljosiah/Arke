# CLAUDE.md

The canonical grounding for this repository lives in [`AGENTS.md`](./AGENTS.md).

`CLAUDE.md` and `AGENTS.md` are intended to resolve to the same guidance (PRD §12.2). On
platforms that support it, replace this file with a symlink to `AGENTS.md`:

```bash
ln -sf AGENTS.md CLAUDE.md
```

Until then, treat `AGENTS.md` as authoritative and keep this pointer in sync.

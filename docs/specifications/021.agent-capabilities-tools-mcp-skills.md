---
spec_id: SPEC-2026-07-03-agent-capabilities-tools-mcp-skills
title: Portable agent capabilities — declarative tools, MCP servers & skills, materialised per harness, edited in the UI
status: draft
branch: feat/spec-021-agent-capabilities
owner: core-maintainers
capabilities: [agent-images, agent-capabilities, harness-integration, agent-editor]
created: 2026-07-03
updated: 2026-07-03
---

# Portable agent capabilities — declarative tools, MCP servers & skills, materialised per harness, edited in the UI

## Why
SPEC-016 made the agent image the portable, parseable unit and this project then adopted Omnigent's
declarative `executor` (harness + model + provider) 100%. But an agent is defined as much by **what it
can do** as by which model it runs — its **MCP servers, code/function tools, sub-agents, and skills**.
Today Arke's image records those only as a thin `ToolDecl[] = {name, kind}` and the adapter **drops
them entirely** when it materialises the agent, so a declared MCP server or tool never reaches the
harness. Two further things are missing: Arke has no **source of truth for what each harness natively
supports** (the adapter exposes only coarse feature flags, so "materialise, else degrade" has nothing
to check against), and there is **no way to author or edit an agent's capabilities from the UI** — only
its model can be changed today (SPEC-016 revised). This spec closes all three: the image carries the
full Omnigent-shaped capability declaration, each adapter declares a **capability manifest** and
materialises the declaration into its native config, and the client gains a **rich, capability-aware
agent editor** to create and edit agents (model, permissions, tools, MCP, skills).

## What changes
- ADDED agent-capabilities — the agent image gains a **rich, Omnigent-shaped `tools` map** (keyed by
  name): an **MCP server** (local `command` array or remote `url`/`headers`, an optional exposed-tool
  whitelist, and host-side `environment`), a **function tool** (`callable` + JSON-Schema `parameters`,
  optional sandbox container), or a **sub-agent** (its own `executor`). Replaces the thin `ToolDecl[]`.
  (breaking: yes — `AgentImage.tools` changes shape; see Decision log #1)
- ADDED harness-integration — a **per-adapter `HarnessCapabilities` manifest** (versioned to the
  harness) declaring what the harness natively supports: MCP (local/remote), skills (+ discovery
  locations), function tools, the tool-gating mechanism, and the built-in tool names. It is the source
  of truth materialisation and the editor validate against. (breaking: no)
- ADDED agent-capabilities — an adapter step **`materializeCapabilities`**: each adapter translates the
  normalised tools/MCP/skills into the harness's native configuration, validated against its manifest;
  an unsupported capability is **skipped with a recorded warning**, never silently claimed. (breaking: no)
- ADDED agent-editor — the client can **create a new agent and edit an existing agent's capabilities**
  from the UI — model (exists), per-tool permissions, enabling/adding tools and MCP servers, and adding
  skills — the editor offering only what the connected harness's manifest supports. (breaking: no)
- MODIFIED agent-images — **skills** remain the `skills/<name>/SKILL.md` convention (matching Omnigent,
  Claude Code, and OpenCode) but are now **materialised** to harnesses that support them (for OpenCode,
  discovered directly from `.opencode/skills/` or `.claude/skills/`). (breaking: no)
- MODIFIED human-in-the-loop — Arke's `permission: {read|edit|bash|webfetch|<tool>: allow|ask|deny}`
  remains the **portable gate** (mapped to SPEC-012, and to OpenCode's `permission` field including
  `mymcp_*` wildcards); Omnigent's code-handler `policies` are carried as an **opaque passthrough**
  honoured only by the Omnigent harness, never required of a native harness. (breaking: no)

---

## Requirements

### Requirement: The agent image declares capabilities as a rich, keyed tools map
`capability: agent-capabilities` · `delta: ADDED (feat/spec-021-agent-capabilities)`

The system SHALL parse an agent image's `tools:` as a **keyed map** (Omnigent-shaped, not an array),
where each entry is one of: an **MCP server** (`type: mcp` with either a local `command` + optional
`args`/`environment` or a remote `url` + `headers`, and an optional `tools:` whitelist of exposed tool
names); a **function tool** (`type: function` with a `callable` reference and optional JSON-Schema
`parameters`, `runtime`, and sandbox `container_image`); or a **sub-agent** (`type: agent` carrying its
own `executor`). The loader SHALL retain enough of each entry to reconstruct a working configuration
for a harness, and SHALL reject a malformed entry with a clear `AgentImageError` rather than silently
dropping it. An entry is **malformed** when: it has no recognised `type`; a `type: mcp` entry has
neither `command` nor `url` (or has both); a `type: function` entry has neither `callable` nor
`runtime: client`; or a `type: agent` entry has no `executor`. An empty `environment`/`headers` map and
any absent optional field are valid.

#### Scenario: An MCP server declaration round-trips with its spawn wiring
- **WHEN** an image declares `tools.github: { type: mcp, command: uv, args: [run, python, -m, pkg.github_mcp], tools: [search_issues] }`
- **THEN** the loaded `AgentImage` carries the MCP entry as `{ transport: "local", command: "uv", args: [...], tools: ["search_issues"] }` — its command, args, and exposed-tool whitelist intact
- **AND** a harness adapter can spawn that MCP server from the parsed entry alone.

#### Scenario: A function tool retains its callable and parameter schema
- **WHEN** an image declares `tools.summarize: { type: function, callable: pkg.tools.summarize, parameters: {...} }`
- **THEN** the loaded image carries the `callable` and the JSON-Schema `parameters` for that tool.

#### Scenario: A malformed tool entry fails loudly
- **WHEN** an image declares a `tools.x` with `type: mcp` but neither a `command` nor a `url`
- **THEN** loading the image raises an `AgentImageError` naming the offending tool.

### Requirement: Credentials for tools and MCP servers stay host-side
`capability: agent-capabilities` · `delta: ADDED (feat/spec-021-agent-capabilities)`

The system SHALL keep every credential a tool or MCP server needs on the host, never inline in the
committed image (NFR-1, extending SPEC-016). Rather than scan values heuristically for "secrets", the
loader SHALL apply a **deterministic, name-based rule**: any value in a **credential-bearing field** —
the `Authorization` header, and any `headers`/`environment` key whose name matches (case-insensitively)
`authorization | token | api[-_]?key | secret | password | credential | bearer` — MUST be either a
`${VAR}` interpolation or a host-side profile reference; a literal in such a field is rejected. A literal
in a non-credential field (e.g. `Content-Type: application/json`, a version pin, a non-sensitive env
value) is permitted. **The `${VAR}` reference SHALL be written *unresolved* into the harness's native
config and resolved by the harness at process-spawn time** — Arke SHALL NOT resolve a `${VAR}` to its
literal and write that literal into any file. In particular the resolved secret SHALL NOT appear in the
tracked `opencode.json` (OpenCode natively interpolates `${VAR}` in `mcp.*.environment`/`headers` from
the host environment when it spawns the server). The resolved value exists only in the spawned process's
environment and SHALL never reach the client.

#### Scenario: A literal in a credential-named field is rejected; a benign literal passes
- **WHEN** an image declares `tools.docs: { type: mcp, url: ..., headers: { Authorization: "Bearer sk-LITERAL", Content-Type: "application/json" } }`
- **THEN** loading raises an `AgentImageError` naming the `Authorization` header and directing the author to `${VAR}` or a host-side profile
- **AND** the same header written as `Authorization: "Bearer ${DOCS_TOKEN}"` loads, with `Content-Type: application/json` accepted as a benign literal.

#### Scenario: The resolved secret never lands in a tracked file
- **WHEN** an MCP server with `headers.Authorization: "Bearer ${DOCS_TOKEN}"` is materialised for OpenCode
- **THEN** the written `opencode.json` contains the literal string `${DOCS_TOKEN}`, not the resolved token
- **AND** inspecting `opencode.json` (a tracked file) after materialisation reveals no credential value.

#### Scenario: An interpolated secret resolves only on the host
- **WHEN** the same header is written as `Authorization: "Bearer ${DOCS_TOKEN}"`
- **THEN** the image loads
- **AND** the token is resolved from the host environment at materialisation
- **AND** neither the reference nor the resolved value appears in any client-facing projection.

### Requirement: Each adapter declares a versioned HarnessCapabilities manifest
`capability: harness-integration` · `delta: ADDED (feat/spec-021-agent-capabilities)`

The system SHALL expose, per harness adapter, a **`HarnessCapabilities` manifest** that is the source
of truth for what that harness natively supports, versioned to the harness release. The manifest SHALL
declare at minimum: whether **MCP** is supported and in which forms (local / remote);
whether **skills** are supported and their discovery locations; whether **function tools** are
supported (and sandboxing); the **tool-gating mechanism** (e.g. OpenCode's `permission` field with
`<mcp>_*` wildcards); and the harness's **built-in tool names**. The manifest is authored from the
harness's documentation and MAY be augmented at runtime where the harness exposes a live listing, but
SHALL be usable without the harness running. `materializeCapabilities` and the agent editor SHALL both
validate against it.

#### Scenario: The OpenCode manifest reflects the real harness
- **WHEN** the coordinator loads the OpenCode adapter's `HarnessCapabilities`
- **THEN** it reports MCP `local` + `remote` supported, skills supported (discovered from `.opencode/skills/` and `.claude/skills/`), tool-gating via `permission` wildcards, and the built-in tool set (`bash, edit, write, read, grep, glob, lsp, apply_patch, skill, todowrite, webfetch, websearch, question`).

#### Scenario: The manifest is available without a live harness
- **WHEN** the harness process is not running
- **THEN** the manifest still resolves from the adapter's static declaration so the editor can offer accurate options offline.

### Requirement: Each adapter materialises capabilities into its native config
`capability: harness-integration` · `delta: ADDED (feat/spec-021-agent-capabilities)`

The system SHALL translate the normalised tools/MCP/skills of an agent image into the connected
harness's **native configuration** when the agent is materialised, validated against that harness's
`HarnessCapabilities`. For the OpenCode adapter this SHALL: write each MCP server into `opencode.json`'s
`mcp` block as `{ type: "local", command: [...], environment: {...}, enabled: true }` (local) or
`{ type: "remote", url, headers, enabled: true }` (remote); express per-agent tool enablement through
the agent's **`permission`** field (using `<mcp>_*` wildcards for a server's tools — the per-agent
`tools` map is deprecated); and rely on OpenCode's native skill discovery (place `SKILL.md` under a
discovered path) rather than a config entry. The Omnigent adapter SHALL pass the Omnigent-shaped
declaration through near-verbatim. A capability a harness does not support SHALL be **skipped with a
recorded `capability.unsupported` warning**, never silently presented as supported.

#### Scenario: An OpenCode agent's MCP server lands in opencode.json
- **WHEN** an image with `tools.github: { type: mcp, command: [...] }` is materialised against the OpenCode adapter
- **THEN** `opencode.json` gains an `mcp.github` entry of `type: local` with that `command` array and `enabled: true`
- **AND** the agent's `permission` enables (or gates) `github_*` per the image.

#### Scenario: An unsupported capability degrades gracefully
- **WHEN** an image declares a capability the connected harness's manifest does not list (e.g. a function tool with a `container_image` the harness does not sandbox)
- **THEN** materialisation completes for everything else
- **AND** a `capability.unsupported` warning is recorded naming the tool and the harness
- **AND** the projection never claims the unsupported capability is active.

### Requirement: Skills are the SKILL.md convention and materialise where supported
`capability: agent-images` · `delta: MODIFIED (feat/spec-021-agent-capabilities)`

The system SHALL discover an agent's skills from `skills/<name>/SKILL.md` (each with `name` +
`description` frontmatter and a progressive-disclosure body), matching Omnigent, Claude Code, and
OpenCode. On a harness that supports skills the adapter SHALL make them discoverable to the agent —
for OpenCode, by ensuring the `SKILL.md` folders sit under a location OpenCode already searches
(`.opencode/skills/`, `.claude/skills/`, `.agents/skills/`, project or global); on a harness with no
skills mechanism the skills SHALL remain metadata and the loader SHALL NOT fail. Skills are never
invented in YAML — the directory is the source of truth.

#### Scenario: A skill is discovered and materialised for OpenCode
- **WHEN** an agent directory contains `skills/debate/SKILL.md` with `name: debate`
- **THEN** the loaded image lists the `debate` skill with its description
- **AND** materialising against OpenCode leaves the `SKILL.md` in a discovered path so OpenCode's `skill` tool can load it.

#### Scenario: A skill-less harness does not fail on skills
- **WHEN** an image with skills is materialised against a harness that has no skills mechanism
- **THEN** materialisation succeeds and a warning records that skills were not projected to that harness.

### Requirement: Arke's permission block is the portable gate; Omnigent policies are opaque passthrough
`capability: human-in-the-loop` · `delta: MODIFIED (feat/spec-021-agent-capabilities)`

The system SHALL treat the image's `permission: {read|edit|bash|webfetch|<tool>: allow|ask|deny}` as the
**portable, harness-agnostic** authorisation posture — a **static declaration**, distinct from SPEC-012's
runtime flow. At materialisation the adapter SHALL translate it into the harness's native gate (for
OpenCode, the agent's `permission` field, including `<mcp>_*` wildcards). At **runtime**, a tool whose
posture is `ask` causes the harness to raise a permission request, which then flows through the SPEC-012
`permission.asked` → decide loop (once/always/reject). So the static field *configures* the gate; SPEC-012
*runs* it — they are two levels, not the same mechanism. Omnigent's
code-handler `policies` (function `handler` on `[request|response|tool_call|tool_result]`) SHALL be
carried on the image as an **opaque passthrough** honoured only by the Omnigent harness; a native
harness SHALL NOT be required to execute them and SHALL ignore them with a recorded note. Arke SHALL NOT
execute arbitrary policy handler code itself.

#### Scenario: Permission gates a tool on a native harness
- **WHEN** an agent with `permission: { bash: ask }` runs a shell tool on the OpenCode harness
- **THEN** the action routes through the SPEC-012 permission flow (once/always/reject) exactly as today.

#### Scenario: Omnigent policies pass through untouched
- **WHEN** an image carries `policies.pii_guard: { type: function, handler: ..., on: [response] }` and runs on the Omnigent harness
- **THEN** the policy declaration reaches Omnigent verbatim
- **AND** on a native harness the same declaration is ignored with a recorded note, and the image still loads and runs.

### Requirement: An operator can create a new agent from the UI
`capability: agent-editor` · `delta: ADDED (feat/spec-021-agent-capabilities)`

The system SHALL let an operator create a new agent from the client. Creation SHALL write a valid
`agents/<name>/config.yaml` under the project (name validated as a safe slug, confined to the project
root), seeded with an `executor` (harness + a chosen model, per SPEC-016 revised), an interaction mode,
and a default `permission` posture; it MAY scaffold an empty `skills/` directory. The new agent SHALL
appear in the roster immediately (the registry reloads and the projection refreshes), and its harness
SHALL be one the coordinator has a `HarnessCapabilities` manifest for.

#### Scenario: Creating an agent adds it to the roster
- **WHEN** an operator creates an agent `researcher-2` on the `opencode` harness with model `github-copilot/gpt-5.5`
- **THEN** `agents/researcher-2/config.yaml` is written with that executor and a default permission block
- **AND** the roster lists `researcher-2` without a reload.

#### Scenario: An unsafe or duplicate name is refused
- **WHEN** an operator tries to create an agent whose name is not a safe slug, or that already exists
- **THEN** creation is refused with a clear reason and nothing is written.

### Requirement: An operator can edit an agent's capabilities from the UI, offered only what the harness supports
`capability: agent-editor` · `delta: ADDED (feat/spec-021-agent-capabilities)`

The system SHALL let an operator edit an existing agent's capabilities from the client: its **model**
(SPEC-016 revised, existing), its **per-tool permissions** (allow / ask / deny), which **tools** are
enabled, its **MCP servers** (add / remove a local or remote server), and its **skills** (add or
reference a `SKILL.md`). The editor SHALL be **capability-aware** — it offers only the tool kinds, MCP
forms, and skill support that the connected harness's `HarnessCapabilities` manifest declares, and it
SHALL surface built-in tool names from the manifest for the permission controls. All edits SHALL be
persisted to the agent's `config.yaml` (host-side), validated (NFR-1: an MCP credential is captured as
a `${VAR}` reference or host profile, never a literal secret), and reflected in the roster live.

#### Scenario: Adding an MCP server through the editor
- **WHEN** an operator adds a local MCP server `github` (command + a `${GITHUB_TOKEN}` env reference) to an agent on the OpenCode harness
- **THEN** the agent's `config.yaml` gains the `tools.github` MCP entry with the `${GITHUB_TOKEN}` reference (not a literal)
- **AND** on next materialisation the server lands in `opencode.json` and the roster shows `github (mcp)`.

#### Scenario: The editor hides options the harness cannot express
- **WHEN** the connected harness's manifest does not support remote MCP servers
- **THEN** the editor does not offer a "remote MCP" option for an agent on that harness.

#### Scenario: A literal secret is refused at edit time
- **WHEN** an operator pastes a literal token into an MCP header field
- **THEN** the edit is refused with guidance to use a `${VAR}` reference or a host profile, and nothing is written.

### Requirement: The client surfaces an agent's declared capabilities without leaking secrets
`capability: agent-capabilities` · `delta: ADDED (feat/spec-021-agent-capabilities)`

The system SHALL show, in the agent roster/editor, each agent's declared capabilities — tool names and
kinds (mcp / function / agent), MCP endpoints by name, and skills by name — so an operator can see what
an agent can do. The projection SHALL carry no credential value, no `${VAR}` resolution, and no
credentials reference; it is names and kinds only.

#### Scenario: The roster lists an agent's tools and skills
- **WHEN** an operator opens the roster for an agent with a `github` MCP tool and a `debate` skill
- **THEN** the roster shows `github (mcp)` and the `debate` skill
- **AND** the response contains no token, header value, or credentials reference.

---

## Design

### Architectural decision
The agent image is the **single normalised, Omnigent-shaped description** of an agent's capabilities;
each harness adapter (a) publishes a **`HarnessCapabilities` manifest** of what it natively supports and
(b) is the **materialiser** that translates the declaration into the harness's native dialect. The
Omnigent adapter is near pass-through. Arke keeps its own concise `permission` block as the portable
authorisation gate (it maps cleanly to SPEC-012 and to OpenCode's `permission` field), and treats
Omnigent's code-handler `policies` as an Omnigent-only opaque passthrough — Arke never runs arbitrary
handler code. The UI agent editor is **driven by the manifest**, so it can only ever offer capabilities
the connected harness can actually deliver. Rationale: adopting Omnigent's structure 100% keeps the
image portable; the manifest gives materialisation and the editor a single, offline-usable source of
truth; the permission/policies split preserves NFR-1 and avoids coupling native harnesses to Python
policy code.

### Target architecture
`@arke/agent-image` parses `tools` (keyed map), `skills` (directory), `policies` (opaque passthrough),
and `os_env` into a typed `AgentImage`, and gains a **config writer** (generalising the SPEC-016-revised
`setAgentModel`) that edits `config.yaml` surgically. (`params` and `terminals` are **out of scope for
this spec** — deferred until a harness consumes them; an image carrying them still loads.) `@arke/contracts` defines the
tool union (`McpTool | FunctionTool | AgentTool`), the passthrough `Policy`, and the
**`HarnessCapabilities`** type. Each `HarnessAdapter` exposes `capabilitiesManifest(): HarnessCapabilities`
and a `materializeCapabilities(image)` step invoked by `materializeAgent`: the OpenCode adapter writes an
`mcp` block into `opencode.json`, expresses tool gating through the agent's `permission` field, and
relies on native skill discovery; the Omnigent adapter emits the tools map verbatim. The coordinator
adds ops: `harness.capabilities` (return the manifest), `agent.create` (write a new image), and an
extended `agent.configure` (edit tools/permissions/skills/mcp, not only the model). The client's agent
editor (extending the SPEC-016-revised model chip) reads the manifest to render capability-aware
controls. The dispatch path (SPEC-016 model override) is unchanged.

### Data model
- `McpTool` is a **discriminated union** on a `transport` tag the loader sets, so `command` XOR `url` is
  enforced by the type (not left to a runtime check):
  ```ts
  type McpTool =
    | { type: "mcp"; transport: "local";  command: string; args?: string[]; environment?: Record<string,string>; tools?: string[]; enabled?: boolean; description?: string }
    | { type: "mcp"; transport: "remote"; url: string; headers?: Record<string,string>; tools?: string[]; enabled?: boolean; description?: string }
  ```
  The **source YAML** carries no `transport` — the loader infers it (`command` present → `local`, `url`
  present → `remote`; neither/both → the malformed error in FR-01). The normalised local form keeps
  Omnigent's `command` (executable) + `args` (array); the OpenCode adapter flattens `[command, ...args]`
  into OpenCode's single `command` array at materialisation.
- `FunctionTool { type:"function", callable?, runtime?: "client", parameters?, containerImage?, description? }` —
  a `function` tool has a `callable` unless `runtime: "client"`.
- `AgentTool { type:"agent", executor, prompt?, osEnv?, passHistory?, maxSessions? }`. **Precedence:** the
  directory form (`agents/<name>/config.yaml` → `subAgents`) is canonical; an inline `tools.<name>:
  {type: agent}` with the **same** name is de-duped (the directory wins); an inline sub-agent with a
  unique name is added to `subAgents`. There is exactly one merged `subAgents` list.
- `Tools = Record<string, McpTool | FunctionTool | AgentTool>` on `AgentImage`, replacing `ToolDecl[]`.
  (The existing directory discovery — `tools/{python,typescript,mcp}/` — folds into the same keyed map.)
- `Skill { name, description?, path }` (existing `SkillRef`, from `skills/<name>/SKILL.md`).
- `Policy { name, type, handler, on: ("request"|"response"|"tool_call")[], … }` — carried **opaque**; not
  executed by Arke (only the Omnigent harness honours it).
- `HarnessCapabilities { harness, version?, mcp: { local: boolean, remote: boolean }, skills: { supported: boolean, locations: string[] }, functionTools: boolean, sandbox: boolean, toolGating: "permission" | "none", builtinTools: string[] }`.
  *(`tools-map` gating was dropped — OpenCode's per-agent `tools` map is deprecated in favour of
  `permission`; no supported harness uses a per-agent tools map, so the option had no consumer.)*

### Interfaces and contracts
- `AgentImage.tools: Tools` (keyed map) — the breaking shape change from `ToolDecl[]`.
- `HarnessAdapter.capabilitiesManifest?(): HarnessCapabilities` and
  `HarnessAdapter.materializeCapabilities?(image): Promise<{ registered: string[]; unsupported: { name, reason }[] }>`
  — both **optional** (like `materializeAgent?` today), so existing adapters keep compiling; an adapter
  with no manifest is treated as supporting nothing (all capabilities degrade with a warning).
- OpenCode `opencode.json` `mcp` schema: `{ [name]: { type:"local", command: string[], environment?: Record<string,string>, enabled? } | { type:"remote", url, headers?: Record<string,string>, enabled? } }`;
  per-agent tool gating via the agent's `permission` field (incl. `<mcp>_*` wildcards). **The OpenCode
  adapter today writes only `.opencode/agents/<name>.md`; this spec adds create-or-merge management of the
  project `opencode.json` `mcp` block, preserving other keys.**
- Coordinator ops: `harness.capabilities` → `HarnessCapabilities`; `agent.create { name, harness, model, mode? }`.
  `agent.configure` is **extended** from today's `{ name, provider, model, reasoningEffort }` (SPEC-016
  revised) to also accept `{ permission?, tools?, skills? }`; `provider`/`model`/`reasoningEffort` are
  retained (reasoningEffort folds into the image's `executor.config.options`).
- Trace records: additive kinds `capability.materialised { agent, harness, registered }` and
  `capability.unsupported { agent, harness, capability, reason }`, written through `Trace.write` so each
  acquires SPEC-015's `at`/`seq`/`projectId` via the WriteQueue (consistent with SPEC-015 conventions).

### Cross-cutting
- **Security / NFR-1:** tool and MCP credentials are host-side only; the loader and the editor reject a
  literal in a credential-named field. A `${VAR}` reference is written **unresolved** into the harness
  config (never resolved-and-written into a tracked file such as `opencode.json`); the harness resolves
  it from the host environment at process-spawn time, so the literal secret lives only in the spawned
  process. MCP servers
  spawn processes: their **tool invocations are gated by the agent's `permission` posture at runtime**
  (via SPEC-012). `os_env.sandbox` remains **advisory** (as in SPEC-016 — carried, not yet enforced);
  actually confining a spawned MCP process to the sandbox is **out of scope for this spec** and noted as
  a follow-up, so this spec does not claim sandbox enforcement.
- **Observability:** every capability registration and every `capability.unsupported` skip is written to
  the trace (SPEC-015).
- **Design template:** the agent editor UI follows the `arke-design` skill (`.claude/skills/arke-design/`)
  — it extends the SPEC-016-revised model editor into a capability-aware panel (permissions, tools, MCP,
  skills), rendering only what the connected harness's manifest supports.

---

## Tasks
- [ ] Contracts: add the `McpTool | FunctionTool | AgentTool` union + `Tools` keyed map, the opaque
  `Policy` type, and the `HarnessCapabilities` type; change `AgentImage.tools` from `ToolDecl[]` to `Tools`.
- [x] Loader (`@arke/agent-image`): parse the keyed `tools` map (mcp/function/agent), `policies`,
  `params`, `terminals`; reject inline secrets (NFR-1) and malformed entries; keep `skills/` + recursive
  `agents/` discovery; add a surgical `config.yaml` writer for tools/permissions/skills (generalise `setAgentModel`). *(Tools writer `setAgentTools` added — write-then-validate-then-rollback, NFR-1-safe. Permission/mode writers already in. Skills stay directory-driven — Decision #3, not a YAML writer.)*
- [ ] Adapter (OpenCode): publish `capabilitiesManifest()` (docs-derived, versioned) and implement
  `materializeCapabilities` — create-or-merge the `mcp` block in `opencode.json` (writing any `${VAR}`
  **unresolved**, never a resolved secret), express tool gating via `permission` wildcards, rely on
  native skill discovery, and record `capability.unsupported`.
- [ ] Adapter (Omnigent): publish its manifest + pass the tools/policies/os_env map through verbatim.
- [ ] Coordinator: add `harness.capabilities`, `agent.create`, and the extended `agent.configure`; fold
  the materialisation result into the trace + the client-safe roster projection (names + kinds only).
- [x] Client: extend the agent editor (SPEC-016-revised) into a capability-aware panel — create agent;
  edit permissions/tools/MCP/skills; render options from the manifest; refuse literal secrets. *(Edit mode now edits MCP tools too — loads the current wiring via `agent.get`, renders the same `ToolEditor` as create, validates against the manifest, and preserves non-MCP tools on save. Skills remain read-only/directory-driven — Decision #3.)*
- [ ] Migration: the bundled roster agents declare no rich tools today (only `tools: []`), so no data
  migration is needed for them; but the `ToolDecl[] → Tools` change is breaking for the *type* and the
  loader's existing `tools/{python,typescript,mcp}/` directory discovery must fold into the keyed map —
  update both and add a fixture that loads a pre-change image.

### Testing
- Unit (loader): mcp/function/agent entries round-trip with full wiring; malformed entries rejected per
  the enumerated rules; a literal in a credential-named field (`Authorization`, `*_token`, `api_key`, …)
  is rejected while a benign literal (`Content-Type`) and a `${VAR}` pass; skills discovered; config
  writer edits surgically.
- Unit (adapter): the OpenCode manifest reports the documented built-ins/MCP/skills; materialising an
  MCP tool writes the expected `opencode.json` `mcp` entry + `permission` gating; an unsupported
  capability yields a `capability.unsupported` record and does not abort the rest.
- Integration: `agent.create` + extended `agent.configure` add an agent with an MCP server and it lands
  in native config; the roster projection carries names/kinds and no secret; the editor validates against the manifest.
- Verify: end-to-end against a running OpenCode + a small local MCP server — create/edit an agent in the
  UI, materialise, and drive an MCP tool call through the coordinator.

### Definition of done
- All scenarios for the requirements above pass under review.
- `AgentImage.tools` is the keyed Omnigent-shaped map; every adapter publishes a `HarnessCapabilities`
  manifest; the OpenCode adapter materialises MCP + tool-gating + skills into native config and records
  unsupported capabilities; the Omnigent adapter passes them through; the UI can create and edit agents
  (model/permissions/tools/MCP/skills), offering only manifest-supported options; credentials never
  leave the host and never reach the client.
- Full workspace typecheck + tests green; a reviewer confirms (i) NFR-1 holds for tool/MCP credentials
  and (ii) the shapes are consistent with the **in-tree** `docs/design/omnigent-agent-yaml-adoption.md`
  and this spec's own cited external references (Omnigent `AGENT_YAML_SPEC.md` at
  github.com/omnigent-ai/omnigent, OpenCode docs at opencode.ai/docs/{mcp-servers,tools,skills}). *(The
  external sources are cited by URL, not vendored; verification against a live upstream is not a merge
  gate — a follow-up MAY vendor excerpts under `docs/reference/` if we want an in-tree check.)*

---

## Decision log
| # | Decision | Rationale |
|---|----------|-----------|
| 1 | Replace the thin `ToolDecl[]` with Omnigent's **keyed `tools` map** (mcp/function/agent) carrying full wiring | The array captured only name+kind, so nothing could be materialised; adopting Omnigent's structure 100% keeps the image portable and makes tools actually work (breaking, but the surface is new/unused) |
| 2 | Keep Arke's `permission` block as the portable gate; carry Omnigent `policies` as **opaque passthrough** | `permission` maps cleanly to SPEC-012 **and to OpenCode's native `permission` field (incl. `<mcp>_*` wildcards)**; Arke cannot/should not execute arbitrary Python policy handlers, so `policies` only mean something on Omnigent |
| 3 | Skills stay the `skills/<name>/SKILL.md` **directory convention** | Per the Omnigent repo (`examples/debby/skills/*/SKILL.md`) and the OpenCode docs (opencode.ai/docs/skills — OpenCode auto-discovers `SKILL.md` from `.opencode/skills/` and `.claude/skills/`); matches Claude Code + Arke's existing discovery, so materialising to OpenCode needs no config entry. *(External sources cited by URL, not vendored.)* |
| 4 | Tool/MCP credentials are **host-side only** (NFR-1), via `${VAR}` interpolation or a host profile | Extends the SPEC-016 credential boundary to tools; an inline literal secret in a committed image (or an editor field) is a hard error |
| 5 | An unsupported capability **degrades with a recorded warning**, never silently | A harness that cannot express a capability must not present it as active — the projection stays honest |
| 6 | Each adapter owns a **`HarnessCapabilities` manifest** (docs-derived, versioned, offline-usable) as the source of truth | Arke has no generic way to enumerate a harness's native tools; a static, versioned manifest lets materialisation validate and the editor offer only real options without the harness running |
| 7 | Per-agent tool gating on OpenCode uses the **`permission` field**, not the deprecated per-agent `tools` map | The OpenCode docs (opencode.ai/docs/agents) mark per-agent `tools: {name: bool}` deprecated in favour of `permission` (and the current adapter's `agentMarkdown` already emits `permission`, not `tools`); Arke's existing `permission` block maps directly onto it. *(Cited by URL, not vendored.)* |
| 8 | The UI agent editor is **manifest-driven** (capability-aware) | The editor must never offer a capability the connected harness cannot deliver; reading the manifest guarantees the options match reality |
| 9 | Secret safety is a **deterministic name-based rule**, not value scanning | "Is this string a secret?" is undecidable; requiring `${VAR}`/profile only in credential-*named* fields (Authorization, `*token`, `api_key`, secret, password, …) is deterministic, lets benign literals (`Content-Type`) through, and still enforces NFR-1 |
| 10 | The `agents/<name>/` **directory is the canonical sub-agent**; an inline `tools.agents` entry de-dupes to it | Two representations (`subAgents[]` vs a `tools.<name>: {type: agent}`) exist in Omnigent; a single merged list with the directory winning avoids ambiguity |
| 11 | `capabilitiesManifest?` / `materializeCapabilities?` are **optional** adapter methods; a missing manifest supports nothing | Keeps existing adapters compiling (like `materializeAgent?`); an unimplemented adapter degrades every capability with a warning rather than breaking the build |
| 12 | A `${VAR}` reference is written **unresolved** into the harness config; the harness resolves it at spawn (never resolve-and-write) | Resolving a secret and writing it into the tracked `opencode.json` would leak it into git — a direct NFR-1 violation (the review caught this). OpenCode natively interpolates `${VAR}` in `mcp.*.environment`/`headers`, so pass-through is both safe and native. |
| 13 | `McpTool` is a **discriminated union** (`transport: local\|remote`), the loader inferring it from `command` vs `url` | All-optional fields cannot enforce the `command` XOR `url` invariant; a tagged union makes malformed entries a type error and keeps Omnigent's `command`+`args` shape without the array/scalar ambiguity |

## Open questions
Genuinely unresolved (must close before `approved`):
- **Editor v1 scope:** full CRUD on sub-agents (`type: agent`) in v1, or start with mcp/function tools +
  skills + permissions and defer sub-agent authoring?
- Adopt Omnigent's `self` / `inherit` tool semantics in this spec, or defer to a follow-up?
- Confirm whether OpenCode's server API exposes a **live tool listing** we could use to augment the
  manifest (the local server was down during authoring) — decides only *augmentation*, not the design.

**Decided (moved out of Open questions):** manifest freshness → **static, offline-usable, MAY augment at
runtime** (FR-03 / decision #6); `terminals`/`params` → **deferred, out of scope** (Target architecture);
`container_image`/`sandbox` enforcement → **out of scope; `os_env.sandbox` stays advisory** (Cross-cutting).

**Design alternative (not a blocker):** the editor may live in the (currently hidden) roster screen
promoted for this, or as an overlay from the cockpit model chip — a UI-placement choice, decided at build.

## Change history
- 2026-07-09 · feat/spec-021-agent-tools-editor · draft — Codex-review fixes (PR #73): the tools round-trip was lossy/over-eager. (a) `agent.get` now returns the **top-level `config.yaml` tools only** (new loader `readConfigTools`), excluding directory-discovered tools — re-writing a discovered function tool inline would fail validation. (b) The editor only sends tools when the operator **actually edited them** (`toolsDirty`), so a model/permission-only save no longer round-trips (and re-materialises) the tools block. (c) When tools ARE edited, the original entry is carried through, preserving unrendered metadata (`enabled` / `tools` whitelist / `description`) and the original `args` array unless the args field was changed (so an arg containing spaces isn't split). (d) `fetchAgentTools` now **throws** on a failed/offline read so the `toolsLoaded` guard leaves tools untouched (was resolving to `{}`, which could clear the block). **Bounded (follow-up):** removing/renaming an MCP tool updates `config.yaml` (source of truth) + the roster, but reconciling the project-wide `opencode.json` `mcp` block (removing a server no agent references) is a separate adapter feature — `materializeCapabilities` is merge-only and per-agent, so a project-wide prune needs all agents' tools; the operator should also drop the matching `<mcp>_*` permission row. Tests added (loader `readConfigTools` excludes discovered tools).
- 2026-07-09 · feat/spec-021-agent-tools-editor · draft — CLOSED the agent-editor gap: an existing agent's **MCP tools are now editable in the UI**, not just at create. `@arke/agent-image` gains `setAgentTools` (surgical `tools:` writer, write-then-validate-then-rollback, NFR-1-safe); the coordinator's `configureAgent` accepts `tools` (writes them, then `materializeCapabilities` so the edited MCP block reaches `opencode.json`, traced) and a new `agent.get` op returns an agent's full editable wiring (the roster projection is names+kinds only; `config.yaml` holds `${VAR}` refs, never secrets); the client editor's edit mode loads that wiring, renders the same manifest-validated `ToolEditor` as create, and **preserves non-MCP (function/agent) tools** on save so they are never dropped. Skills stay directory-driven/read-only (Decision #3). Loader + coordinator integration tests added (setAgentTools round-trip/rollback; `agent.configure` writes tools + re-materialises; `agent.get`; literal-secret rejection); verified live in the browser. **Still deferred:** editor CRUD for sub-agents (`type: agent`) and function tools; scaffolding/removing skill directories from the UI.
- 2026-07-03 · feat/spec-021-agent-capabilities · draft — ADDED agent-capabilities (rich tools/MCP map + per-harness materialisation), harness-integration (`HarnessCapabilities` manifest), agent-editor (create/edit agents in the UI); MODIFIED agent-images (skills materialise), human-in-the-loop (permission portable, Omnigent policies passthrough). Authored against the Omnigent `AGENT_YAML_SPEC.md` and OpenCode docs (cited by URL).
- 2026-07-03 · feat/spec-021-agent-capabilities · draft — **two correctness fixes after review round 2**: (NFR-1) a `${VAR}` credential reference is written *unresolved* into `opencode.json` and resolved by the harness at spawn — never resolve-and-write a secret into a tracked file (decision #12, + a scenario); `McpTool` is now a `transport: local|remote` **discriminated union** enforcing `command` XOR `url` (decision #13), with the FR-01 scenario aligned to `command`+`args`.
- 2026-07-03 · feat/spec-021-agent-capabilities · draft — **revised after the cockpit review panel** (opus + sonnet, 42 findings). Grounded on the now-synced SPEC-005/016 (executor model); fixed data-model inconsistencies (`command`+`args`, typed `environment`, `AgentTool` vs `subAgents` precedence, dropped the dead `tools-map` gating, made `capabilitiesManifest?`/`materializeCapabilities?` optional); replaced heuristic secret detection with a deterministic **credential-named-field `${VAR}`** rule; de-conflated the static `permission` declaration from SPEC-012's runtime flow; noted `opencode.json` is newly adapter-managed and reconciled the new trace kinds with SPEC-015; enumerated tool-validation rules; reframed external citations as URL references (not an in-tree merge gate); pruned Open questions of items already answered by requirements.

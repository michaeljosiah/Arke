---
spec_id: SPEC-2026-07-08-auto-pr-configuration
title: Auto-PR configuration — let the implementer open the PR on delivery
status: draft
branch: feat/spec-030-auto-pr-config
owner: core-maintainers
capabilities: [single-session-delivery, project-config]
created: 2026-07-08
updated: 2026-07-08
relates: [011.session-detail-and-rescue.md, 028.single-session-delivery.md]
---

# Auto-PR configuration — let the implementer open the PR on delivery

## Why
Delivery ([SPEC-028](028.single-session-delivery.md)) ends with the implementer's checklist complete and the card in the diff-review lane, waiting for a human to review the diff and open a pull request ([SPEC-011](011.session-detail-and-rescue.md)). For a trusted, fast-moving project that per-diff gate is friction the engineer may want to trade away — letting the implementer open the PR itself as the final step of delivery. This adds a per-project setting that opts into exactly that, while keeping the human gate as the default.

## What changes
- ADDED project-config — a per-project `delivery.autoOpenPr` preference (default `false`) persisted in `.arke/config.json`, read and written through `delivery.settings` / `delivery.configure` ops
- ADDED single-session-delivery — when `autoOpenPr` is on, the delivery prompt instructs the implementer to open a pull request targeting the spec's feature branch (`gh pr create --base '<featureBranch>' --fill`, base shell-quoted) once every task is checked off
- MODIFIED single-session-delivery — the delivery prompt is now a function of the auto-PR preference; with it off (the default) the prompt is unchanged and delivery still stops at the diff-review gate
- ADDED project-config — a Settings › Governance toggle that flips the preference live

**Relates to:** [SPEC-011](011.session-detail-and-rescue.md) (the diff-review gate this relaxes), [SPEC-028](028.single-session-delivery.md) (the delivery prompt this extends).

---

## Requirements

### Requirement: A per-project auto-PR preference, defaulting off
`capability: project-config` · `delta: ADDED (feat/spec-030-auto-pr-config)`

The system SHALL persist a per-project boolean `delivery.autoOpenPr` in the project's canonical config file `.arke/config.json` (SPEC-005/019 — all project config lives in that one file). The default, when the key is absent or the file is missing/unreadable, SHALL be `false`. Reading it SHALL be exposed as a `delivery.settings` op returning `{ autoOpenPr }`; writing it SHALL be a `delivery.configure { autoOpenPr }` op that performs a read-modify-write PRESERVING every other key in the file (registry, settings, providers, …) and refuses to overwrite a file that exists but is not valid JSON. Because enabling auto-PR is a standing authorization for future deliveries to open PRs without a per-diff human click, `delivery.configure` SHALL record the decision to the audit trace **fail-safe before** applying the config change (`writeOrThrow`, mirroring `approveDraft`): if the audit append cannot land, the config SHALL NOT be flipped.

#### Scenario: Absent config reads as off
- **WHEN** `delivery.settings` is requested for a project whose `.arke/config.json` has no `delivery` block (or no file)
- **THEN** it returns `{ autoOpenPr: false }`

#### Scenario: Configure round-trips and preserves other keys
- **WHEN** `delivery.configure { autoOpenPr: true }` is issued for a project whose config already has a `registry` block
- **THEN** a subsequent `delivery.settings` returns `{ autoOpenPr: true }`
- **AND** the `registry` block (and any `settings`/`providers`) is unchanged on disk

#### Scenario: A malformed config is not clobbered
- **WHEN** `delivery.configure` is issued and `.arke/config.json` exists but is not valid JSON
- **THEN** the write is refused with an error
- **AND** the existing file bytes are left untouched

#### Scenario: The authorization is audited before it takes effect
- **WHEN** `delivery.configure` is issued and the audit trace cannot be written
- **THEN** the op fails and `.arke/config.json` is NOT changed (no un-audited standing authorization)

### Requirement: Auto-PR instructs the implementer to open the PR on delivery
`capability: single-session-delivery` · `delta: ADDED (feat/spec-030-auto-pr-config)`

When a delivery is dispatched ([SPEC-028](028.single-session-delivery.md)) for a project with `autoOpenPr` on, the delivery prompt handed to the implementer SHALL include an instruction to open a pull request once every task in the checklist is checked off, via `gh pr create`. Because the delivery session runs in its worktree on `<featureBranch>--delivery` ([SPEC-028](028.single-session-delivery.md)), the PR SHALL target the spec's **feature branch** (`--base <featureBranch>`) — merging delivery → feature, which then merges to mainline = `delivered` ([SPEC-024](024.specification-lifecycle-gating.md)) — rather than jumping straight to the repository default and bypassing the feature-branch integration point. The base branch SHALL be **shell-quoted** where it is embedded in the command, so a crafted `branch:` frontmatter value cannot inject shell syntax (git ref names disallow spaces but permit `$`, `(`, `)`, `'`). When the feature branch is unknown, the instruction SHALL omit `--base` (falling back to `gh`'s default). When `autoOpenPr` is off (the default), the delivery prompt SHALL contain no PR instruction, and delivery SHALL stop at the human diff-review gate exactly as before (SPEC-011 unchanged). The auto-PR decision in effect for a dispatch SHALL be recorded on the delivery's dispatch trace entry.

This is the propose-decide-execute boundary for code output ([SPEC-011](011.session-detail-and-rescue.md)) reframed rather than removed: the engineer's config choice is a STANDING, pre-authorized decision to execute (open the PR) without a per-diff click. It relaxes SPEC-011's "a PR SHALL never be opened automatically without an explicit human approval on the diff" — the approval is given once, at config time, instead of per delivery.

#### Scenario: Auto-PR on targets the feature branch
- **WHEN** a delivery is dispatched for a project with `autoOpenPr` on and a known feature branch
- **THEN** the implementer's prompt tells it to open a pull request targeting that feature branch (`gh pr create --base <featureBranch> --fill`) once every task is checked off
- **AND** the feature branch is shell-quoted in the command

#### Scenario: A base branch with shell metacharacters is inert
- **WHEN** the feature branch contains shell metacharacters (e.g. `feat/$(whoami)`)
- **THEN** it appears single-quoted in the command, so the metacharacters cannot execute

#### Scenario: Auto-PR off is the unchanged default
- **WHEN** a delivery is dispatched for a project with `autoOpenPr` off (or unset)
- **THEN** the prompt contains no PR instruction
- **AND** the card lands in the diff-review lane for the human to open the PR (SPEC-011)

#### Scenario: The dispatch trace records the decision
- **WHEN** any delivery is dispatched
- **THEN** its `dispatch.started` trace entry carries the `autoOpenPr` value that shaped the prompt

### Requirement: The preference is editable in Settings and seeded on connect
`capability: project-config` · `delta: ADDED (feat/spec-030-auto-pr-config)`

The connection snapshot SHALL carry the active project's `delivery.autoOpenPr` so the client renders the toggle's true state without an extra round-trip. Settings › Governance SHALL present a toggle that reads that state and, on change, issues `delivery.configure` (a governed write — refused, not queued, while offline). The toggle MAY update optimistically, but when the write is refused or fails it SHALL roll back to the previous value and surface a notice, so the toggle never reads "on" while the persisted config still reads `false`. The default runtime mode toggle and the rest of the governance surface SHALL be unaffected.

#### Scenario: Toggle reflects the persisted value on connect
- **WHEN** a client connects to a project with `autoOpenPr` on
- **THEN** the Settings auto-PR toggle shows on

#### Scenario: Flipping the toggle persists via the coordinator
- **WHEN** the engineer flips the auto-PR toggle
- **THEN** the client issues `delivery.configure` with the new value
- **AND** the next delivery for that project reflects the new preference

#### Scenario: A failed persist rolls the toggle back
- **WHEN** the engineer flips the toggle but the `delivery.configure` write is refused (e.g. offline) or fails
- **THEN** the toggle returns to its previous value
- **AND** a notice surfaces the failure — the UI never claims a setting that did not persist

---

## Design

### Architectural decision
Auto-PR is delivered **prompt-side, not coordinator-side**: rather than build (currently non-existent) coordinator PR-opening (`git push` + `gh pr create`), the delivery prompt itself instructs the implementer — which runs in the project checkout with git tooling — to open the PR. This keeps the whole feature to one config flag + a conditional prompt fragment, fits single-session-delivery's "the agent decides how to tackle the work" philosophy, and is fully unit-testable via prompt assertions. The trade-off (recorded in the decision log) is that the PR is opened by the agent, outside the coordinator's governed/traced path — acceptable because the engineer explicitly opts in, and the dispatch trace still records that auto-PR was in effect. The PR targets the spec's feature branch (`--base <featureBranch>`): the delivery worktree is wired into the harness cwd (SPEC-028), so the agent is on `<featureBranch>--delivery`, and merging that into the feature branch (which then merges to mainline = delivered) matches the SPEC-024 topology rather than bypassing the feature branch. The base is shell-quoted (`shSingleQuote`) so a crafted `branch:` value cannot inject shell syntax.

### Target architecture
```
Settings toggle ──delivery.configure {autoOpenPr}──► ProjectContext
                                                        └─ setAutoOpenPr(.arke/config.json)  (read-modify-write, preserve keys)

spec.deliver ──► deliverImplementation
                   ├─ loadAutoOpenPr(.arke/config.json)  → boolean
                   ├─ trace dispatch.started { autoOpenPr }
                   └─ buildDeliveryPrompt(specPath, tasks, autoOpenPr ? {autoOpenPr, baseBranch: featureBranch} : {})
                                                              └─ appends "open a PR (gh pr create --base '<featureBranch>' --fill) when done"

connection snapshot ──{ delivery: { autoOpenPr } }──► client store.autoOpenPr ──► Settings toggle state
```

### Data model
No new store. The preference is one boolean under a `delivery` block in the existing `.arke/config.json`:
```json
{ "registry": { … }, "delivery": { "autoOpenPr": true } }
```
`loadAutoOpenPr(configPath)` reads it (false on absent/unparseable/wrong-type — only literal `true` opts in). `setAutoOpenPr(configPath, value)` read-modify-writes it atomically (temp + rename), preserving every other key and refusing to clobber invalid JSON. Both live in `packages/coordinator/src/delivery-config.ts` (side-effecting file I/O, kept out of the pure `delivery.ts`).

### Interfaces and contracts
- Ops (internal, coordinator dispatch table — not `@arke/contracts`): `delivery.settings` → `{ autoOpenPr }`; `delivery.configure { autoOpenPr }` → `{ ok, autoOpenPr }`.
- `buildDeliveryPrompt(specPath, tasks, opts?: { autoOpenPr?, baseBranch? })` — the third argument is optional, so existing callers are unaffected; `shSingleQuote` (exported from `delivery.ts`) shell-quotes the base branch.
- Connection snapshot gains `delivery: { autoOpenPr }`.
- Client: `setAutoOpenPr(value)` in `live.ts` (governed write + optimistic store update); `store.autoOpenPr` seeded in `applySnapshot`.

### Cross-cutting
- **Governance (SPEC-011):** this is the one place Arke lets code output reach a PR without a per-diff human click. It is off by default, opt-in, per-project, and audited on the dispatch trace. The relaxation is explicit and reversible (flip the toggle back).
- **Security:** the config write only ever sets a boolean. The one spec-supplied string that reaches the prompt's command — the feature branch as the PR `--base` — is shell-quoted (`shSingleQuote`), so a crafted `branch:` frontmatter value cannot inject shell syntax into the `gh pr create` command shown to the agent. The PR itself is opened by the agent under its own harness permissions, not by the coordinator.
- **Design template:** the Settings toggle follows the `arke-design` skill (`.claude/skills/arke-design/`) — the existing `Row` + `Switch` pattern in the Governance group.

---

## Tasks
- [x] `delivery.ts`: `buildDeliveryPrompt` gains an optional `{ autoOpenPr, baseBranch }` — appends the PR instruction (`gh pr create --base '<featureBranch>' --fill`, base shell-quoted via `shSingleQuote`) only when `autoOpenPr` is set
- [x] `delivery-config.ts` (new): `loadAutoOpenPr` (default-false, lenient) + `setAutoOpenPr` (atomic read-modify-write, key-preserving, refuses to clobber invalid JSON)
- [x] `project-context.ts`: read the preference in `deliverImplementation`, thread it into the prompt, record it on the `dispatch.started` trace; add `delivery.settings` / `delivery.configure` ops (trace-before-write fail-safe); add `delivery` to the connection snapshot
- [x] Client: `store.autoOpenPr`; seed it in `applySnapshot`; `setAutoOpenPr` governed helper in `live.ts` (optimistic + roll-back-on-failure); a Settings › Governance toggle
- [x] Unit tests (`delivery.test.ts`, `delivery-config.test.ts`, client `auto-pr-toggle.test.ts`): prompt on/off; base-branch targeting + `shSingleQuote` + injection-safety (`feat/$(whoami)`, embedded `'`); config default/round-trip/preserve-keys/refuse-clobber; toggle roll-back on refused write
- [x] Integration tests (`delivery-integration.test.ts`): `delivery.configure` → `delivery.settings` round-trip persisting to `.arke/config.json`; auto-PR-off prompt omits the instruction; auto-PR-on prompt targets the shell-quoted feature branch

### Testing
- Unit (`delivery.ts`): default prompt contains no `pull request` / `gh pr create`; `{ autoOpenPr: true }` with no base → `gh pr create --fill` (no `--base`); `{ autoOpenPr: true, baseBranch }` → `gh pr create --base '<branch>' --fill` with the "pre-authorised" framing; a base with shell metacharacters (`feat/$(whoami)`, `o'brien`) is POSIX single-quoted; `shSingleQuote` unit-tested directly.
- Unit (`delivery-config.ts`): absent file → false; `autoOpenPr: true` → true; wrong-typed / missing value → false; unparseable → false (no throw); `setAutoOpenPr` round-trips, preserves `registry`/`settings`/`providers`, and throws (leaving bytes intact) on an existing-but-invalid-JSON file.
- Unit (client `setAutoOpenPr`): a refused offline write rolls the store `autoOpenPr` back to its previous value and surfaces a cockpit notice.
- Integration (`ProjectContext`, real git repo, mock adapter): `delivery.configure { autoOpenPr: true }` returns `{ ok, autoOpenPr }`, `delivery.settings` then reads true, and `.arke/config.json` on disk has `delivery.autoOpenPr: true`; a default delivery's dispatched prompt has no `gh pr create`; an auto-PR-on delivery's dispatched prompt matches `gh pr create --base '<featureBranch>' --fill`.

### Definition of done
All scenarios pass; coordinator suite green (325 tests, incl. the auto-PR unit + integration tests); client suite green (44); full typecheck clean across coordinator, client, and desktop; the Settings toggle is verified to flip and persist against a live coordinator.

---

## Decision log
| # | Decision | Rationale |
|---|----------|-----------|
| 1 | Prompt-driven, not coordinator-driven, PR opening | Avoids building non-existent coordinator `git push` + `gh pr create`; one flag + a conditional prompt fragment; fits single-session-delivery; fully unit-testable. Trade-off: the PR is opened by the agent, outside the governed/traced coordinator path (the dispatch trace still records that auto-PR was in effect) |
| 2 | Off by default, opt-in, per-project | Preserves SPEC-011's human diff gate as the safe baseline; the relaxation is a deliberate, reversible choice for a trusted project, not a global default |
| 3 | Stored in `.arke/config.json` under a `delivery` block | The canonical single project config file (SPEC-005/019); a dedicated block avoids colliding with `settings` (adapter/process-advisory) and keeps the file hand-editable |
| 4 | Read-modify-write preserves other keys; refuses to clobber invalid JSON | The config file is hand-editable and holds the registry; a setter that dropped other keys — or overwrote a half-edited file — would be a footgun |
| 5 | Auto-PR targets the feature branch (`--base <featureBranch>`), shell-quoted | With the worktree wired (SPEC-028) the agent is on `<featureBranch>--delivery`; merging delivery → feature (which then merges to mainline = delivered) matches the SPEC-024 topology rather than jumping `--delivery` → mainline and bypassing the feature branch. The base is a spec-supplied string, so it is shell-quoted (`shSingleQuote`) — git ref names permit `$()`/`'`, which unquoted could inject into the `gh pr create` command shown to the agent. (This revises the earlier `--fill`-only form from PR #64, which was correct only while the worktree was unwired.) |
| 6 | `delivery.configure` records the authorization fail-safe (`writeOrThrow`) BEFORE applying the config change | Enabling auto-PR is a standing authorization for future PRs-without-per-diff-approval; it must not take effect with no durable audit record if the trace is unwritable — same governed-write discipline as `approveDraft` (caught in PR review) |
| 7 | The Settings toggle rolls back on a failed/refused persist | An optimistic toggle left "on" after a refused write would claim a setting the project config doesn't hold, so the next delivery wouldn't open a PR despite the UI saying it would (caught in PR review) |

## Open questions
- ~~The SPEC-028 delivery worktree is not wired…~~ **Resolved** (SPEC-028: the implementer runs in the `<featureBranch>--delivery` worktree). ~~The residual auto-PR **base** question…~~ **Resolved**: auto-PR now targets the feature branch (`gh pr create --base '<featureBranch>' --fill`), so delivery → feature → mainline follows the SPEC-024 topology; the base is shell-quoted for injection safety.
- The agent needs `gh` auth + a configured remote to actually open the PR; when neither is present the instruction is a best-effort no-op (the agent reports it couldn't). A future coordinator-side, host-optional PR-open (mirroring SPEC-024's local-merge fallback) could make this deterministic and bring it back under the governed/traced path — deferred.
- Should auto-PR also imply auto-approval of the diff gate on the board (so the card doesn't sit in the diff lane), or is the opened PR itself the signal? Left as-is for now: delivery still marks the card `done` via the checklist oracle; the PR is extra.

## Change history
- 2026-07-08 · feat/spec-030-auto-pr-config · draft — ADDED per-project `delivery.autoOpenPr` (default off) in `.arke/config.json` via `delivery.settings`/`delivery.configure` ops; `buildDeliveryPrompt` appends a `gh pr create --fill` instruction when on; the preference rides the connection snapshot and a Settings › Governance toggle. Prompt-driven (the implementer opens the PR), a standing config-time relaxation of SPEC-011's per-diff gate, off by default.
- 2026-07-08 · docs/renumber-auto-pr-031 · draft — RENUMBERED 030 → 031: this spec and the cross-repo-specifications spec (PR #63) both merged to `main` claiming `030` (concurrent branches — this one branched before #63 landed). Per first-merged-wins, #63 keeps `030` and this spec moves to `031`; the file was renamed and SPEC-011's cross-reference updated. The `feat/spec-030-auto-pr-config` branch name in the delta tags is left as the true provenance of the change.
- 2026-07-08 · feat/spec-030-auto-pr-config · draft — PR review fixes (Codex, PR #64, 4 findings): (1) dropped the interpolated `--base <featureBranch>` — the harness runs in the project checkout on the feature branch (the delivery worktree isn't wired to the adapter cwd), so `gh pr create --fill` (feature → repo default) is both correct and injection-safe; (2) `setAutoOpenPr` (client) now rolls the toggle back and surfaces a notice on a refused/failed write; (3) `delivery.configure` records the authorization fail-safe (`writeOrThrow`) before flipping the config; (4) doc corrected re: which branch the PR opens from. +1 client rollback test; prompt/integration tests updated. Coordinator 325/325, client 45/45, full typecheck clean.
- 2026-07-08 · feat/spec-028-worktree-cwd · draft — amended: the SPEC-028 delivery worktree is now wired into the harness cwd, so the auto-PR reasoning here is corrected — the agent runs on `<featureBranch>--delivery` (not the feature branch), and `gh pr create --fill` opens `--delivery` → the repo default. The `--fill` form (no interpolated `--base`) is kept for injection-safety; whether auto-PR should target the feature branch instead is now an open follow-up. No code change to this spec's feature.
- 2026-07-08 · feat/spec-031-autopr-base · draft — MODIFIED the auto-PR base: now that the delivery session runs on `<featureBranch>--delivery` (SPEC-028), auto-PR targets the spec's FEATURE branch — `buildDeliveryPrompt` re-gains a `baseBranch` option and emits `gh pr create --base '<featureBranch>' --fill`, so delivery → feature → mainline follows the SPEC-024 topology instead of `--delivery` → mainline. The base is shell-quoted via a new `shSingleQuote` helper (git ref names permit `$()`/`'`), closing the injection concern that drove the earlier `--fill`-only form (PR #64) — the correct base is now known AND safely quoted. +unit tests (feature-branch targeting, `shSingleQuote`, `feat/$(whoami)`/`o'brien` injection-safety), integration test updated. Coordinator 329/329, client 45/45, full typecheck clean.
